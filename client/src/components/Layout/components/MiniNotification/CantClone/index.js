import styles from './CantClone.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function CantCloneDialog() {
    return (
        <div>
            
        </div>
    );
}

export default CantCloneDialog;