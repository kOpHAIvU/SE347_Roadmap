import classNames from 'classnames/bind';
import styles from './AdvanceLevelTwo.module.scss';

const cx = classNames.bind(styles);

function AdvanceLevelTwo() {
    return (
        <div
            className={cx('level-two')}>

        </div>
    );
}

export default AdvanceLevelTwo;